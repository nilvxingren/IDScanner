<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="frameBack" parent="@android:style/Widget.Button">
        <item name="android:height">@dimen/back_frame_height</item>
        <item name="android:width">@dimen/back_frame_width</item>
    </style>

    <style name="frameFront" parent="@android:style/Widget.Button">
        <item name="android:height">@dimen/front_frame_height</item>
        <item name="android:width">@dimen/front_frame_width</item>
    </style>

</resources>
